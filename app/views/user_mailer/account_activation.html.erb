 <h1><%= t('user_mailer.account_activation.welcome') %></h1>

  <p>
  <%= t('user_mailer.account_activation.greeting', name: @user.name) %>
  </p>

  <p>
  <%= t('user_mailer.account_activation.instructions') %>
  </p>

  <%= link_to t('user_mailer.account_activation.activate_account'),
      edit_account_activation_url(@user.activation_token, email: @user.email) %>

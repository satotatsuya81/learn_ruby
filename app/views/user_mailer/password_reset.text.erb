<%= t('user_mailer.password_reset.greeting', name: @user.name) %>

  <%= t('user_mailer.password_reset.body') %>

  <%= link_to t('user_mailer.password_reset.reset_link'),
      edit_password_reset_url(@user, reset_token: @user.reset_token) %>

  <%= t('user_mailer.password_reset.ignore_message') %>

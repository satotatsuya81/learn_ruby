<%# 名刺編集ページ %>
<% content_for :title, "名刺編集" %>

<div class="container">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <h1 class="h3 mb-4">名刺編集</h1>

      <!-- React BusinessCardForm コンポーネント -->
      <div data-controller="business-card-form"
           data-business-card-form-mode-value="edit"
           data-business-card-form-card-value="<%= @business_card.to_json %>"
           data-business-card-form-errors-value="<%= @business_card.errors.full_messages.to_json %>">

        <!-- Loading表示（Reactがマウントされるまでの間） -->
        <div class="text-center">
          <div class="spinner-border" role="status">
            <span class="visually-hidden">読み込み中...</span>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
<div class="container">
    <%= link_to t('application.name'), root_path, class: "navbar-brand" %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
    <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav me-auto">
            <li class="nav-item">
            <%= link_to t('navigation.home'), root_path, class: "nav-link" %>
            </li>
        </ul>
        <ul class="navbar-nav">
            <% if logged_in? %>
                <li class="nav-item">
                    <span class="navbar-text me-2">
                        <%= t('navigation.welcome_message', name: current_user.name) %>
                    </span>
                </li>
                <li class="nav-item">
                    <%= link_to t('navigation.profile'), user_path(current_user), class: "nav-link" %>
                </li>
                <li class="nav-item d-flex align-items-center">
                    <%= button_to t('navigation.logout'), logout_path, method: :delete, class: "nav-link" %>
                </li>
            <% else %>
                <li class="nav-item">
                    <%= link_to t('navigation.login'), login_path, class: "nav-link" %>
                </li>
                <li class="nav-item">
                    <%= link_to t('navigation.signup'), signup_path, class: "nav-link" %>
                </li>
            <% end %>
        </ul>
    </div>
</div>
</nav>
